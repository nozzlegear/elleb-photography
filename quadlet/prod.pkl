/// Production environment configuration
module prod

extends "base.pkl"

const environment: Environment = new Environment {
  isDev = false
  themeHostPath = null
  configFilePath = null
  themeName = "elleb-photos"
  hostPort = 3050
  ghostUrl = "https://www.elleb.photography"
  nodeEnv = "production"

  // Optional: Override instance name and credentials
  // Uncomment and modify these to customize your deployment:
  // instanceName = "myghostinstance"
  // secretName = "myghostinstance_secrets"
  // mysqlUser = "myghostuser"
  // mysqlDatabase = "myghostinstance_db"
}
